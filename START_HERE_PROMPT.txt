You are picking up work in this repo.

Before doing anything substantial:
1. If `docs/in-box-codex-guide.md` exists because this package was copied into a fresh box, read that first.
2. Read `AGENTS.md`.
3. Read `README.md`.
4. Read `HANDOFF.md`.
5. Read `user_model.json`.
6. If present, read `HANDOFF.local.md` and `user_model.local.json`.
7. Read `codex_budget_policy.md`.
8. Read `codex_usage_tracker_spec.md`.
9. If you want the compact bootstrap path, read `docs/startup-manifest.md`.
10. If you want the recurring process lessons from recent runs, read `process_learnings.md`.
11. Treat `archive/governor-spike-20260420/` as historical context only; the live implementation is the usage tracker.
12. If you plan to use the portable usage-tracker bundle in a new Codex environment, run `./tools/codex-usage-checkpoint smoke-test` first; it validates wrapper resolution and local telemetry visibility without writing a ledger.
13. For filtered tracker slices after `mark`, keep the default `./tools/codex-usage-checkpoint window` when you want only newly created child sessions; use `./tools/codex-usage-checkpoint window --cutoff-mode updated` when you want the already-running parent thread's post-cutoff activity instead.
14. Summarize the repo objective, working rules, active tools, and any detected local overlays in 8-12 bullets.
15. State uncertainties as hypotheses rather than flattening them.
16. Only then continue the task.

Task continuation rule:
- Match the repo's actual objective, not the locally easiest interpretation.
- Keep decision burden low.
- Preserve anti-anchoring where uncertainty is real.
- For infra/system work: read first, plan briefly, then make small auditable diffs.
- Prefer project-scoped token observability and shadow-credit summaries over hard throttles.
